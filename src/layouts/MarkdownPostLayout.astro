---
import { Picture } from "astro:assets";
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
const { title, pubDate, description, author, image, imageAlt, tags } =
	frontmatter;
---

<BaseLayout pageTitle={title}>
	<p>{pubDate.toLocaleDateString()}</p>
	<p><em>{description}</em></p>
	<p>Written by: {author}</p>
	{
		image && (
			<>
				{typeof image === "string" ? (
					// If 'image' is a string, use a standard <img> tag
					<img src={image} width="300" alt={imageAlt} />
				) : (
					// Otherwise, it's a local image object, so use <Picture />
					<Picture
						src={image}
						widths={[240, 540, 720]}
						sizes="(max-width: 720px) 100vw, 720px"
						alt={imageAlt}
					/>
				)}
			</>
		)
	}

	<div class="tags">
		{
			tags.map((tag: string) => (
				<p class="tag">
					<a href={`/tags/${tag}`}>{tag}</a>
				</p>
			))
		}
	</div>
	<slot />
</BaseLayout>
